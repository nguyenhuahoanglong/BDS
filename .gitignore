# Runtime / per-session state
.claude/scheduled_tasks.lock

# Local agent skill mirror (canonical copy lives in .claude/skills/)
.agents/

# Local HTTP server / scraper logs
logs/

# Local backups created during merge operations
output/*.backup.json
